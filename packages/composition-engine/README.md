# composition-engine

Orchestrates the visible experience for the current task.

It decides which modules are active, which one is primary, what should be collapsed, where the avatar appears, what visualization is promoted, and how the layout adapts to screen and task.

Compositions should be inspectable, serializable and testable.
