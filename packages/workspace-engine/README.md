# workspace-engine

Manages working areas rather than fixed pages.

Responsibilities:
- panel registry;
- docking;
- split layouts;
- floating panels;
- focus mode;
- full-screen surfaces;
- persistence of layouts;
- responsive transformations.

A workspace should be serializable so the same composition can be restored later.
