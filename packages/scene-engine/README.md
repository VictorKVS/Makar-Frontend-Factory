# scene-engine

Owns cinematic 2.5D and 3D presentation.

Responsibilities include scene lifecycle, camera, lighting, environment, model and texture loading, level-of-detail, fallback modes, and GPU/performance instrumentation.

Three.js / React Three Fiber are likely web implementations where appropriate, but contracts should not be tied unnecessarily to one renderer.
