# stream-engine

Normalizes simultaneous information flows.

Canonical classes:
- primary;
- secondary;
- background;
- alert;
- agent.

The engine carries semantics and priority. It must not decide concrete CSS or 3D rendering.
