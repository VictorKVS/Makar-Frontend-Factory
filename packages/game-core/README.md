# game-core

Reserved for reusable game-specific capabilities.

This package intentionally starts small. It must not become a dumping ground for ordinary frontend state.

Potential future scope includes gameplay loop abstractions, simulation clock, save/load contracts, entity state and game session lifecycle.

It may reuse input, scene, UI and avatar packages.
