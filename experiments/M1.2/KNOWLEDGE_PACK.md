# M1.2 — Knowledge Pack

ALINA selected seven items.

1. `makar.composition.engines_remain_independent` — shell composition must not absorb engine responsibilities.
2. `makar.composition.reason_trace` — visible scenario/tier/avatar decisions must have reason tags.
3. `makar.avatar.capability_negotiation` — renderer limitations degrade presentation, not persona state.
4. `makar.stream.attention_budget` — secondary/background streams are bounded.
5. `makar.workspace.state.presentation_separation` — layout state is not domain state.
6. `makar.motion.reduced_motion` — decorative motion has a reduced-motion path.
7. `makar.qa.change_impact_ui_assertions` — replacing the homepage requires explicit test impact review.

No full-library prompt dump was used.
