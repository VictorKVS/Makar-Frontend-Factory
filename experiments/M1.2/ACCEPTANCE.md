# M1.2 — Acceptance

- ALINA product shell is the default route.
- Research is default with `knowledge-graph` as primary.
- Security switches primary to `security-graph` and exposes a qualified alert.
- Presentation switches primary to `visualization` and may request hologram presentation.
- Focus switches primary to `document-or-editor` and hides ALINA while retaining state.
- Core tier keeps the active task usable and falls back from hologram to portrait.
- Command submission updates visible ALINA state.
- Engineering diagnostics are closed by default and remain accessible.
- Keyboard focus is visible.
- Reduced motion disables decorative orbit animation.
- Responsive screenshots are captured at laptop, desktop and ultrawide widths.
- CI quality budget and shell contract validation pass.
