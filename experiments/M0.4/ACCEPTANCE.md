# M0.4 — Acceptance Contract

Accepted when:

- `packages/stream-engine` contains typed serializable contracts;
- ranking is deterministic;
- primary/background/agent/alert semantics are tested;
- acknowledgement is tested;
- interruption policy is tested;
- attention budget is tested;
- no shared stream code imports ALINA/React;
- ALINA demo shows at least five simultaneous semantic stream classes;
- background does not visually displace primary;
- alerts are visibly distinguishable and traceable;
- Playwright proves key attention behavior;
- CI passes;
- visual artifact is produced;
- RESULT.md compares Experiment 003 with earlier context delivery.
