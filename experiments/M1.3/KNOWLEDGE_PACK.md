# M1.3 — Knowledge Pack

ALINA selected six items.

1. `makar.stream.semantic_priority` — transport arrival does not decide visual prominence.
2. `makar.stream.agent_distinction` — agent activity remains distinct from user task data.
3. `makar.composition.engines_remain_independent` — Data Gateway supplies normalized data; Composition still decides arrangement.
4. `makar.stream.attention_budget` — multiple live streams remain bounded.
5. `makar.stream.interruption_policy` — alerts require semantic qualification before interruption.
6. `makar.types.fixture_required_fields` — test fixtures must contain all required contract fields.

## Additional project rule

Every payload must carry provenance so the UI can visibly distinguish LIVE / CACHED / DEMO / SYNTHETIC / UNAVAILABLE.

No full-library dump was used.
